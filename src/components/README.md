# Public Components

Astro/Preact components for the public site (SDD.md §3.2): Header/Nav, Hero, About, AnimalGrid + FilterTabs, AnimalCard, AnimalDetailModal, Gallery + GalleryLightbox, ContactForm, Footer.

Built in M4 (Development-Plan.md), reproducing `design-reference/Scattered Oaks Farms.dc.html`'s visual/interaction spec exactly, backed by live data from `workers/` instead of the hardcoded sample animals in that reference file.
