# Administrator Components

The `/admin` SPA (SDD.md §3.1, §3.2) — new components not present in the public-only design prototype: AdminLogin, AdminShell, AnimalEditor, ContentEditor, SiteSettingsPanel, AdminUserManager, AuditLogView.

Built in M6 (Development-Plan.md), alongside the admin API endpoints in `workers/`.
