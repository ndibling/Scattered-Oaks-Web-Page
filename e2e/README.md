# End-to-End Tests (Playwright)

Visitor, Administrator, and Security flows (SDD.md §8, Requirements.md §12.3) — added in M4 (visitor flow subset) and M8 (full suite, including the browser-driven security flow: lockout, reset via emailed link, forced first-login change).

Nothing here yet — M1 only installs and configures Playwright (`playwright.config.ts`).
